//'\n' means jump to the next line
// if we use 'printf' and we want to jump to the next line, we should use '\n'
// but when you use 'puts', you don't have to use '\n'
#include<stdio.h>

int main(){

    printf("Hello world \n");

    puts("Hello world");

    printf("Hello world");



return 0;
}
